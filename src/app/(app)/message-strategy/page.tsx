import { Newspaper } from "lucide-react";
import { StubPage } from "@/components/shared/stub-page";

export default function Page() {
  return (
    <StubPage
      icon={Newspaper}
      title="Message Strategy"
      description="Top issues raised, issue trends, constituency comparisons, approved talking points and the objection-handling library."
    />
  );
}
