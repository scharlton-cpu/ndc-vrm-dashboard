import { Network } from "lucide-react";
import { StubPage } from "@/components/shared/stub-page";

export default function Page() {
  return (
    <StubPage
      icon={Network}
      title="Relationships"
      description="Lawful, explicitly entered household, family, community and volunteer relationships, shown at aggregate or authorized operational level."
    />
  );
}
